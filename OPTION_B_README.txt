JAIPUR PRINTS — OPTION B

Firebase Auth + Firestore are connected. Firebase Storage is intentionally not used because this setup stays on Spark.

For product images in Admin > Products, paste a public image URL. You can later move to Firebase Storage (Blaze) or another image host if desired.

WhatsApp default: +91 9876543210. Change it in Admin > Settings and save to Firestore.

Run with a local web server (for example VS Code Live Server); do not open the HTML files directly with file://.
